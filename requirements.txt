# Course dependencies (third-party only). Your miniplat/ code is imported from the
# working tree via PYTHONPATH (handled by ./scripts/verify.sh) — not installed as a
# package — so your edits take effect immediately with no reinstall.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
qdrant-client>=1.9
numpy>=1.26
gradio>=4.0
pypdf>=4.0
redis>=5.0
prometheus-client>=0.20
pyyaml>=6.0
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
